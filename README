Setup
-----

It is recommended to use deploy.sh from infra-scripts for automating the deployment.

Otherwise, go to core/, then create cron, enter cron and clone it there, such as:


- core/
|
 - cron
 |
  - panel


To edit while deploying:
------------------------

- in HTML deploy new release: change href
